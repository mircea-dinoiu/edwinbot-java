package commands.users.level_related;

import manager.Command.Command;

public class Commands {
    public static void $level(Command command) {}
    public static void $tradelevel(Command command) {}
}
